# MiMo API Key（必填）
MIMO_API_KEY=your_api_key_here

# MiMo API 地址（可选，默认为小米MiMo）
MIMO_API_URL=https://api.xiaomimimo.com/v1/chat/completions

# 数据库路径（可选，默认为当前目录下的 data.db）
DB_PATH=data.db

# 服务端口（Render 会自动设置 PORT 环境变量）
# PORT=18890

# Flask 调试模式（生产环境不要开启）
# FLASK_DEBUG=false